glob2==0.7
PyPDF2==3.0.0
langchain-community
sentence-transformers==2.2.2
redis==4.5.5
numpy==1.23.5
python-dotenv==1.0.0


Note

Make sure to install the langchain-community package as per the appropriate version for the Ollama and HuggingFaceEmbeddings models, and ensure compatibility with other dependencies. You might need to adjust the version numbers according to your environment and requirements.